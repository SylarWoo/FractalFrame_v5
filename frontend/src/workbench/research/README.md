# Research Module

This folder is the frontend boundary for strategy research and future VectorBT workflows.

Keep research and backtest UI separate from `rightDrawer` and chart runtime modules. Chart modules can render research output, but strategy configuration, run metadata, and result panels should live here.
